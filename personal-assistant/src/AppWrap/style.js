import styled from 'styled-components'

export const Wrap = styled.div `
    background-color: ${({theme}) => theme.colors.background};
    min-height: screen;
    max-width: screen;
    // make span entire window?
`
